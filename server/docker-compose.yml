version: '3.8'
services:
  gnews-api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - API_KEY=test-key
    volumes:
      - .:/app
    command: uvicorn server.main:app --host 0.0.0.0 --port 8000 --reload
